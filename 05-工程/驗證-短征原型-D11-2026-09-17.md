# 驗證｜短征原型 D11｜2026-09-17

負責人：Tycoon 工程實作  
狀態：**工程自證完成／待產品主編 A 層開驗**  
寫碼：`claude --model claude-opus-5 --effort high`（實際標籤：claude-opus-5 + effort high）  
路徑：`/home/box/shared/ai-tycoon/05-工程/short-raid-proto`

## 可跑方式

```bash
cd /home/box/shared/ai-tycoon/05-工程/short-raid-proto
npm i && npm run dev   # http://localhost:5173
npm test
npm run build
```

尚無公開 Pages URL（本輪交付本地可演示）。

## 一輪路徑

1. 任命席 → 任命 WOLF  
2. 出征席 → 派出（status: ready → in_field；約 3 秒壓縮模擬）  
3. 戰報席 → 顯示 realized_pnl／return_pct／max_drawdown／trade_count＋模擬標  
4. 再派（新契約 ready）或撤回（`config.enabled=false`；履歷不刪）

## 自檢結果（工程實跑）

| 項 | 結果 | 證據 |
|----|------|------|
| `npm test` | **通過** 22/22 | 2026-09-17 12:37 Asia/Taipei |
| `npm run build` | **通過** | tsc + vite build |
| D11 32 必填 | **齊** | `REQUIRED_FIELDS` 與 lock 名單一致；測試鎖名與數量 |
| status 枚舉 | **ready／in_field／returned** | 對齊升格規格；合法轉移測試 |
| 四旗標 false | **通過** | okx／real_funds／cancel／parallel |
| 失敗入牆 | **通過** | 測試：失敗後再派，失敗列仍在 |
| 撤回不刪史 | **通過** | 測試：withdraw 後 wall 不變 |
| 重播 | **通過** | 同輸入同結果 → replay_ok；篡改失敗 |

## A 層對照（工程自勾；正式判決屬產品主編）

| # | 檢查 | 工程自證 |
|---|------|----------|
| A1 | 屏序任命→出征→戰報→再派／撤回 | 有（UI＋store） |
| A2 | WOLF 任命非工具箱 | 有 |
| A3 | 同輪可見歸來；無盯盤台 | 有（3s 壓縮） |
| A4 | 四績效欄＋模擬源 | 有 |
| A5 | 模擬源常駐；okx/real false | 有 |
| A6 | 無取消入口 | 有 |
| A7 | 單線 | 有 |
| A8 | 再派第二趟；撤回不停履歷 | 有 |
| A9 | 失敗入牆不可洗白 | 有 |
| A10 | 32 必填＋結算閘 | 有 |

## 不做項自證

無 OKX／真資／跟單；無 XP／等級；無排行榜主畫面；無參數／prompt 工具箱；無 K 線盯盤；撤回不刪史；不以舊出征閉環 demo 為準據。

## 待產品／規則確認（不擋 A 演示）

1. `is_official`：~~待產品確認~~ **已裁定（產品主編 2026-09-17）**：失敗且 `replay_ok`＋結算閘通過時 **可為 true**（official＝可稽核證據，非「成功」）。維持現況閘，不改。  
2. `planned_duration=3`、`simulation_principal=10000` 為原型預設（規格允許數值後補）。  
3. `constraint_snapshot` 內含 max_drawdown_limit_pct=6、max_trade_count=30（欄內值，非新欄）。  
4. `result_status` 細名：`completed`／`failed_risk`／`failed_data`／`failed_timeout`／`failed_constraint`（規格允許細枚舉後補）。

## 缺口

- 未做瀏覽器點擊全程錄屏（單元測試覆蓋狀態機／牆／重播；UI 首屏曾由寫碼端 headless 看過）。  
- 未部署公開 URL。  
- B／C 層（60 秒／自願再派）屬產品主編，工程不代判 Go。

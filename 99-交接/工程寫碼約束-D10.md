# 工程寫碼約束（D10）

日期：2026-09-17  
授權：Wayne → Mack 轉達

## 硬規則
1. AI Tycoon **寫代碼一律**使用 box 上已登入的 **Claude Code CLI**。
2. 模型：**(Claude Code) Opus 5 High**。若 CLI／帳號可見名稱不同，選最接近的 Opus 5＋High；並回報實際選到的標籤。
3. **不要**預設用 Cursor Cloud Agent 當主寫碼路徑（例外需 Wayne／Mack 書面授權）。
4. Grok Bot（工程實作）負責：收規格、驗收、跑測試／建置、回報證據；長碼實作交給 Claude Code。
5. 契約欄位未 lock 前仍**不得**猜欄位開工；D10 只鎖定「誰寫碼」，不解除 T5 待命。

## 開工前自檢
- `claude` 可用且已登入
- 能選到 Opus 5 High（或等價檔位）
- 工作目錄與 repo 路徑已對齊 `/home/box/shared/ai-tycoon/` 或指定工程目錄

## 實際 CLI 對齊（工程自檢 2026-09-17）
- 指令：`claude --model claude-opus-5 --effort high`
- 顯示名：Opus 5（無單獨 model id 叫「Opus 5 High」；High＝`--effort high`）
- 可見其他：`claude-sonnet-5`、`claude-haiku-4-5-20251001`、`claude-fable-5-1`
- 自檢：`05-工程/D10自檢-2026-09-17.md`

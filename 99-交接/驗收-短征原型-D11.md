# 驗收｜短征原型 D11
負責人：Tycoon 產品主編  
日期：2026-09-17 12:40 Asia/Taipei  
清單：`01-範圍/驗收清單-短征原型-D11.md`  
工程自證：`05-工程/驗證-短征原型-D11-2026-09-17.md`  
可跑：`05-工程/short-raid-proto` → `npm run dev` → http://localhost:5173

## 判決（先看這）
| 層 | 結果 |
|----|------|
| **A 可演示** | **通過** |
| **B 60 秒理解** | **通過（產品主編可點自測）** |
| **C 自願再派** | **未通過**（無真人小樣紀錄） |
| **產品 Go（B∧C）** | **未通過** |

狀態標籤：**可演示／未通過 Go**（與工程標籤一致；正式 Go 等小樣 C）。

---

## A｜路徑與契約
| # | 檢查 | 結果 | 證據 |
|---|------|------|------|
| A1 | 任命→出征→（出征中）→戰報→再派／撤回 | **過** | 可點走查完整一輪＋再派＋撤回 |
| A2 | WOLF；任命非工具箱 | **過** | 「這是你養的交易員——不是工具箱裡的開關」 |
| A3 | 同輪短征歸來；無盯盤 | **過** | ~3s 壓縮；出征中僅「等待戰報」disabled |
| A4 | 四績效＋模擬源 | **過** | 例：+285.84／+2.86%／4.24%／24 筆＋模擬標 |
| A5 | 模擬常駐；okx／real false | **過** | 頂欄「無真實資金・不連 OKX」；旗標測試 |
| A6 | 無作戰中取消 | **過** | UI「單線出征・不支援取消」；cancel=false |
| A7 | 單線 | **過** | parallel=false；出征中明示單線 |
| A8 | 再派第二趟；撤回不停履歷 | **過** | 第二趟 ct_000002；撤回後牆仍 2 筆 |
| A9 | 失敗／虧損入牆不可洗白 | **過** | UI：虧損戰報入牆；單元測試：`failed_*` 入牆且再派後仍在 |
| A10 | D11 32 必填＋結算閘 | **過** | 產品主編複跑 `npm test` 22/22、`npm run build` 過；名單鎖測 |

### 禁項掃描
未見：OKX 串接／真資／跟單／XP／排行榜主畫面／工具箱主體驗／K 線盯盤／洗白刪史。  
僅見合規提示「無真實資金・不連 OKX」（非串接）。

### 產品裁定（已回工程）
失敗且 `replay_ok`＋結算閘通過 → `is_official` **可為 true**（官方＝可稽核證據，非成功）。閘維持。

### 複跑命令（產品主編）
```
cd /home/box/shared/ai-tycoon/05-工程/short-raid-proto
npm test   # 22/22
npm run build
```

---

## B｜60 秒（可點自測）
依動線 §6 口述腳本，對可點原型自測：四步可串硬句要旨  
「我養自己的 AI 交易員：部署→模擬出征→看戰報→決定再派或撤回。」  
**通過（自測）**。冷啟動小樣尚未跑——不單獨構成 Go。

## C｜自願再派
**無紀錄** → 未通過。開跑條件已滿足（可點短征原型＋契約 lock＋短征）。

## 紀錄
| 場次 | 類型 | A | B | C | 紅線 | 判決 |
|------|------|---|---|---|------|------|
| 2026-09-17 | 工程演示＋產品可點走查＋單元複跑 | 過 | 過（自測） | 無 | 無 | 可演示／未 Go |

## 下一步
1. 產品主編開小樣 n＝3～5（腳本見 `99-交接/60秒口述自測-動線D7-2026-09-17.md` §2）→ 填 C  
2. C 有自願再派紀錄後，依 D6 重判 Go／No-go  
3. 公開 URL／Pages 非 A 門檻；需要演示給外人時再請工程部署

## 給 Mack
A **通過**。Go **未過**（缺 C）。工程可維持「可演示／未代判 Go」。

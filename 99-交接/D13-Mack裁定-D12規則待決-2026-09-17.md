# D13 — Mack 裁定｜D12 規則影響待決（2026-09-17）

決策者：Mack（依 Wayne D12 鎖定＋不改 32 原則；影響執行／不擴大資源）  
授權依據：D12 已 lock；系統規則影響表請求裁定  
狀態：**已裁定**

## 裁定

### R1 — `profession_id`／`skill_id`
- **本輪：後補選填，不升必填，不進 32。**
- 職業三欄語意以 `constraint_snapshot`＋展示對齊為準。
- 升必填條件：產品＋工程證明索引／主檔對齊缺 id 會擋驗收時，再經 Mack／Wayne 另 lock。

### R2 — `constraint_snapshot` 最小 schema
- **寫死建議鍵名於 D12 補丁（行為約束）**，不新增頂層必填欄：
  - `ability`（能力）
  - `applicable_context`（適用情境）
  - `limits`（限制／禁區）
- 派出閘／結算閘：快照非空，且上述三鍵存在且可稽核（值可極薄字串）。
- 工程可加其他鍵；**不得缺這三鍵**當 D12 迭代驗收。
- 不改 32 名單；`constraint_snapshot` 仍為既有必填欄。

### R3 — 擴展說明併入方式
- **另標「D12 規格補丁」**為準據檔，D11 lock 正文與 32 表**不動**。
- 准後動作：把 `出征契約-D12擴展說明-待准.md` 升格為  
  `出征契約-D12規格補丁.md`（狀態＝已准），並在 D11 規格檔首加一行「D12 語意見補丁」。
- 不把大段改寫進 D11 正文，以免凍結合混。

## 准許
- **准** D12 擴展說明（含 R1–R3）生效為規格補丁。
- 工程：仍等 McQueen IA 可實作切片後再開；實作時依補丁，**勿改 32 名單**。

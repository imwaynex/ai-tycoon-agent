# 成長對照文案【定稿｜可嵌 UI｜切片 A】
負責人：Tycoon 世界敘事  
對齊：`01-範圍/成長薄片-產品確認.md`  
狀態：定稿（可嵌 UI；數字僅占位）  
前提：六槽定稿仍有效；空牆維持槽⑥

## 可講占位
上次：`{prev_realized_pnl}` `{prev_return_pct}` `{prev_max_drawdown}` `{prev_trade_count}`  
這次：`{realized_pnl}` `{return_pct}` `{max_drawdown}` `{trade_count}`  
差異（引擎算、敘事不發明公式）：`{delta_realized_pnl}` `{delta_return_pct}` `{delta_max_drawdown}`

---

### G0｜僅一筆戰績（尚不能對照）
WOLF 只有一筆戰績。  
再派一次，才看得出帝國在變強還是變弱。

### G1｜兩次對照（主槽｜截圖核心）
同一隻 WOLF，兩次出征。  
上次：realized_pnl {prev_realized_pnl} · return_pct {prev_return_pct} · max_drawdown {prev_max_drawdown}  
這次：realized_pnl {realized_pnl} · return_pct {return_pct} · max_drawdown {max_drawdown}  
差在：pnl {delta_realized_pnl} · 報酬 {delta_return_pct} · 回撤 {delta_max_drawdown}。  
成長看戰績，不看經驗值。

### G2｜這次更好（可選語氣｜仍須顯示數字）
這次比上次 Sharp。  
差在數字上——再派，讓優勢繼續累積。

### G3｜這次更差（可選語氣｜仍須顯示數字）
這次沒贏過上次。  
差在數字上——再派，才有下一次翻盤。

### G4｜空牆（同六槽⑥｜首次紅線）
戰績牆還是空的。  
派 WOLF 出去，才有故事可截。

---

## 禁寫
- XP、等級、徽章、信用分、聲譽升級
- 虛構「變強了」而無 BattleResult 對照
- 預置假歷史當成長原料
- 圍觀／訂閱／榜單語氣（留給之後 B，且仍禁）

## 驗收自檢
旁觀者截圖須看出：同一隻 WOLF、兩次真實數字、好／差說得出來——不是發經驗值。

## 空牆用字（2026-09-16 微修｜不開新範圍）
定稿用字：**可截**（「才有故事可截」）。  
UI 若出現「可載」＝錯字，請改回「可截」。非 blocker，屬小修。
